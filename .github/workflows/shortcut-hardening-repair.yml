name: Repair shortcut hardening

on:
  push:
    branches:
      - feature/upstream-full-integration
    paths:
      - tools/shortcut-hardening-repair.py
      - .github/workflows/shortcut-hardening-repair.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout integration branch
        uses: actions/checkout@v7
        with:
          ref: feature/upstream-full-integration
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Apply shortcut hardening repairs
        run: python tools/shortcut-hardening-repair.py

      - name: Commit repaired runtime, locale, and tests
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No repair changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/shortcuts.js locales/fr.js tools/check-upstream-integration.mjs
          git commit -m "fix: harden shortcut icon persistence and validation"
          git push origin HEAD:feature/upstream-full-integration

      - name: Run customization checks
        run: node tools/check-customizations.mjs

      - name: Run release metadata checks
        run: node tools/check-release-metadata.mjs

      - name: Run upstream integration checks
        run: node tools/check-upstream-integration.mjs
